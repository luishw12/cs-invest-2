import {Pages} from "@/types/enum/pages";

const programs = [
  {
    id: Pages.DASHBOARD,
    name: "Dashboard",
    link: "/dashboard",
  },
  {
    id: Pages.USERS,
    name: "Usuários",
    link: "/users",
  },
];

export default programs;
